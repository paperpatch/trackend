<div class="container">
  <div class="row">
    <div class="col">
      {{> navigation}}
    </div>
    <div class="col">
      <ol class="post-list">
        {{#each posts as |post|}}
        <li>
          {{> post-info post }}
        </li>
        {{/each}}
      </ol>
    </div>
  </div>
  
</div>