{{#if tickets.length}}
<section class="dashboard">
  <h2>My Tickets</h2>
  <ol>
    {{#each tickets as |ticket|}}
    
    <li>
      {{> ticket-front ticket}}
      <a href="/dashboard/edit/{{ticket.id}}" class="edit-link">Edit ticket</a>
    </li>
    {{/each}}
  </ol>
</section>
{{/if}}