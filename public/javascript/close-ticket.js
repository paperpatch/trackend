const statusOpen = {
    open: true,
    status: 'Open'
};

const statusClosed = {
    open: false,
    status: 'Closed'
}

function closeTicket() {
    let statusOpen = true;
    
    statusOpen = statusOpen ? true : false;
    console.log('ticket set to false');

    

};

  